extend type Query {

	"Obtain a team by its unique id"
	team(id: Int): Team

}

"""
Represents a team of members within vindigo
"""
type Team {
	id: Int!
	name: String!
	description: String!
	createdAt: String!
	logoImage: String
	members: [Profile!]!
}